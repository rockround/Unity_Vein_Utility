using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StructureObject : OrganObject
{
    // Start is called before the first frame update

    // Update is called once per frame
    internal override void fire()
    {
        base.fire();

    }
}
